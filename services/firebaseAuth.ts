/**
 * Firebase auth bootstrap placeholder.
 *
 * Final implementation options:
 * 1) Native Firebase auth modules with Expo development builds.
 * 2) Web Firebase SDK fallback where appropriate.
 *
 * This file centralizes the chosen Firebase auth wiring so phone-auth
 * screens/hooks do not depend on provider-specific setup details.
 */
export const firebaseAuthReady = false;

